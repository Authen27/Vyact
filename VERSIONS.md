# Vyact — Master Version History

> **Parent / master changelog.** Per-app detail lives in each app's own `CHANGELOG.md`; this document is the single index that tells you what's installed, what shipped where, and what's coming next.

## Three deployables in this repo

| App | Path | Current | Live URL | Per-app changelog |
|---|---|---|---|---|
| **Consumer (React)** | `react/` | **v9.5.7** | https://vyact-twentyx.vercel.app | [`react/CHANGELOG.md`](react/CHANGELOG.md) |
| **Admin** | `admin/` | **v1.2.0** | https://vyact-admin.vercel.app | [`admin/CHANGELOG.md`](admin/CHANGELOG.md) |
| **Database (Supabase)** | `supabase/migrations/` | **td-08-09-13-honest-residuals** | n/a — auto-applied by `deploy.yml` via `supabase db push` (TD-20 / PR #16) | [`db/MIGRATIONS.md`](db/MIGRATIONS.md) |
| **Vanilla shell (archived)** | removed in v7.0.1 | **v5.0** *(final)* | archived — see git history | [§ Vanilla shell history](#vanilla-shell-history-v10--v50) below |

> **Production URL note (updated 2026-05-30):** the live URLs above are the production domains of the `react` / `admin` projects under the `bhushandandolus-projects` Vercel team that `deploy.yml` deploys to. The previously-documented `react-taupe-xi.vercel.app` / `finflow-admin.vercel.app` are **orphaned on a different Vercel account**, are not updated by CI, and serve stale builds — reclaiming those exact hostnames needs dashboard action on that other account. See [`DEPLOY.md`](DEPLOY.md) for the authoritative deploy process.

The three apps deploy independently and are versioned independently. The vanilla shell was archived from the working tree in v7.0.1 after the React consumer became the only active app surface; its source remains available in git history.

---

## Cross-app release timeline

Newest first. For full per-version detail, follow the link in the **App** column.

| Date | App | Version | Headline |
|---|---|---|---|
| 2026-06-17 | [Consumer](react/CHANGELOG.md#v943--store-god-module-eliminated-data-core--indexts-2026-06-17) | **v9.4.3** | **Store god-module eliminated.** TD-25 structural completion (behaviour-neutral): the 1,167-line `store.ts` git-mv'd to `store/slices/dataSlice.ts` (verbatim) + a new 33-line `store/index.ts` composition root folding the 7 domain slices. `useStore` byte-identical; tracked as a 93% rename. tsc · 161 tests (money invariants) · build green. Residual: dataSlice 723 lines (CRUD sub-split optional); TD-24 read-sweep/diagnostics remain. |
| 2026-06-17 | [Consumer](react/CHANGELOG.md#v942--customer-feedback-round-2026-06-17) | **v9.4.2** | **Customer feedback round.** EMI re-amortisation via `applyPayment`, debt-payment convergence, investment accounts, drill-downs, savings. |
| 2026-06-15 | [Consumer](react/CHANGELOG.md#v941--store-slice-composition-cloudauth--sync-2026-06-15) | **v9.4.1** | **Store slice-composition: cloudAuth + sync.** Continues TD-25 (behaviour-neutral). Extracted `cloudAuthSlice` (session/myRole + setSession/refreshHouseholds) and `syncSlice` (theme/toasts/lastSyncedAt + manualRefresh/subscribeRealtime/setTheme/toast) verbatim; `store.ts` 861→718 (zero lint warnings). Housekeeping: git remote → Authen27/Vyact.git. Data-core split + index.ts rename + TD-24 read-sweep/diagnostics remain. tsc · eslint · 161 tests · build green. |
| 2026-06-15 | [Consumer](react/CHANGELOG.md#v940--maintainability--observability-release-2026-06-15) | **v9.4.0** | **Maintainability & observability release.** Behaviour-neutral hardening resolving the partner-review cluster (TD-23…TD-28). Fault taxonomy `lib/faults.ts` (expected vs unexpected/dropped-write, silent-write-loss paths instrumented); sync queue extracted to `lib/sync/` (hybridAdapter 508→400, byte-identical); store god-module → Zustand slices begun (`store.ts` 1,167→861: modal/reconcile/notify/recurring slices + shared localJson + typed testHooks); risk-bearing `as any` removed on money + MFA paths; README version truth + CI `version-drift-check.mjs` guard. +CON-UNIT-066…077 (suite 153→161). No consumer behaviour change. tsc · eslint · 161 tests · build green. |
| 2026-06-14 | [Consumer](react/CHANGELOG.md#v933--budget-identity-owned-by-the-database-the-real-fix-2026-06-14) | **v9.3.3** | **Budget identity owned by the database (the real fix).** Retires the v9.3.1 client-side deterministic-id approach (which broke delete+recreate via tombstone revive, and collided with recovered random-id rows). New `upsert_budget(h,b,mode)` RPC is the single writer for all entry points: `create` = INSERT ON CONFLICT(identity) DO NOTHING → raises `BUDGET_EXISTS` (race-proof, catches another member's unsynced budget); `replace` = DO UPDATE + revive. App routes creates through `createBudgetChecked`; form surfaces "already exists". One budget per (household, scope, period); delete+recreate replaces; second member is told it exists. Online required for create. Validated via auto-rollback scenario harness (all PASS). −CON-UNIT-065 +CON-UNIT-067/068. tsc · 152 tests · build green. |
| 2026-06-14 | [Consumer](react/CHANGELOG.md#v932--budget-create-reaches-the-cloud-not-null-period-fix-2026-06-14) | **v9.3.2** | **Budget create reaches the cloud (NOT-NULL `period` fix).** Follow-up to v9.3.1: a newly created budget (e.g. July 2026) still didn't appear on other devices because the create never reached the cloud. `budgets.period` is a legacy `NOT NULL DEFAULT 'monthly'` column the scope-based v9.1 form never sends, but `budgetToRow` mapped it to an **explicit `null`** → every new-budget INSERT violated NOT NULL → the optimistic write threw, dead-lettered, and showed only on the creating device. Fix: `budgetToRow` defaults `period` to `'monthly'`. +CON-UNIT-066. tsc · 154 tests · build green. |
| 2026-06-14 | [Consumer](react/CHANGELOG.md#v931--budget-multi-device-convergence-root-cause-fix-2026-06-14) · [Database](supabase/migrations/20260614130000_v931_budget_identity_convergence.sql) | **v9.3.1** | **Budget multi-device convergence (root-cause fix).** Budgets weren't syncing across a household's devices (one device's wiped, another's never appeared, a third mismatched). Root cause: a budget's identity is `(household, scope, year, month)` (enforced by `uq_budget_month`/`uq_budget_annual`), but the client minted a **random `uid()`** per new budget → two devices, two PKs for the same slot → second `INSERT` violates the unique index and **dead-letters** (silent loss). Fix: deterministic container id (`lib/budgetIdentity.ts`, same principle as recurring) so devices converge via `ON CONFLICT (id)`. DB (`20260614130000`): `replace_budgets` rewritten (it had stripped `scope/period_year/period_month` on restore and PK-collided on same-id re-insert) → schema-correct + `ON CONFLICT (id) DO UPDATE`; dropped the legacy competing `budgets_household_category_uniq` index. +CON-UNIT-065. tsc · 153 tests · build green. |
| 2026-06-14 | [Consumer](react/CHANGELOG.md#v930--whatsapp-business-integration-connection-foundation-2026-06-14) · [Database](supabase/migrations/20260614120000_whatsapp_connection_foundation.sql) | **v9.3.0** | **WhatsApp Business integration — connection foundation.** First slice: the Meta ↔ Vyact connection + the per-user phone-link plug-in (transaction use-cases deferred). DB: `profiles` WhatsApp linkage columns + RLS-locked `whatsapp_verification_otps` / `whatsapp_inbound_messages` (service-role only). Edge Functions: `whatsapp-webhook` (verify-token handshake + constant-time HMAC + ack-first idempotent log), `whatsapp-send-otp` / `whatsapp-verify-otp` (authed link: cooldown, 5-attempt lockout, phone uniqueness). App: Settings → WhatsApp panel. CI: `deploy-edge-functions` job. Dormant until Meta secrets/template/webhook configured ([runbook](whatsapp-connection-setup.md)). tsc · 149 tests · build · boot green. |
| 2026-06-13 | [Consumer](react/CHANGELOG.md#v920--sync-hardening-refresh-based-convergence-r1r5-2026-06-13) | **v9.2.0** | **Sync hardening (refresh-based, R1–R5).** Fixes the growing cross-device sync gaps for the refresh-based model ([`docs/SYNC_FIXPLAN.md`](docs/SYNC_FIXPLAN.md)): **R1** delta-sync now converges — `listSince` `>`→`>=` (no same-ms skip) and soft-deletes bump `updated_at` so tombstones propagate (kills the ghost-row / net-worth case-7 bug); **R2** deterministic recurring-instance ids stop duplicate transactions across devices; **R3** retires the misconfigured `postgres_changes` channel for focus/visibility/online/poll refresh triggers; **R4** honest sync-status badge (surfaces the previously-silent `sync_failed`) + tap-to-refresh full-sweep + "last synced"; **R5** dead-lettered conflicts/failed are re-appliable, not silently lost. **R6** (atomic reconcile) deferred with a column-verified RPC spec. No money-model change. tsc · 149 tests · build · boot green. |
| 2026-06-13 | [Consumer](react/CHANGELOG.md#v912--budgets-monthlyannual-only-remove-custom--recurring-build-fix-2026-06-13) · [Database](supabase/migrations/20260613120000_budget_scope_drop_custom.sql) | **v9.1.2** | **Budgets monthly/annual only + Recurring build fix.** Removed the custom date-range budget scope (confusing, no clear household use-case) — `BudgetScope` is now `month\|annual`, with a validated CHECK constraint `ck_budget_scope` so a stale client can't reintroduce it. Also fixed a build-breaking duplicated component body in `Recurring.tsx` (`TS1128`) that was failing every prod deploy. tsc + 147 tests + build + boot green. |
| 2026-06-13 | [Consumer](react/CHANGELOG.md#v9101--cross-device-consistency-bugfixes-2026-06-13) | **v9.1.01** | **Cross-device consistency bugfixes.** Recurring list order is now deterministic (UI sort + explicit recurring read order in Supabase adapter), Dashboard cash-flow month window now keys to local calendar month (reduces UTC-boundary drift across devices), and budget progress no longer false-empties on transient `budgetAllocations` fetch failures (`refresh()` preserves prior allocation snapshot). No schema change. |
| 2026-06-12 | [Consumer](react/CHANGELOG.md#v910--feedback-batch-budgets-redesign-recurring-rrule-deep-links-account-split-removal-2026-06-12) · [Database](supabase/migrations/20260612120000_v91_budgets_recurring_receivables_deeplink.sql) | **v9.1.0** | **Feedback batch (9 items).** §4 Budgets redesigned with strict identity (scope+year+month, unique constraints — fixes cross-device divergence, Investigation A) + `budget_allocations` child table + recurring forecast. §5 Recurring → RFC-5545 RRULE + owner + investment type + ends-condition; recurrence removed from the txn form (single source of truth); active/deactivate toggle removed; instances link via `recurring_schedule_id`. §6 receivable create UI confirmed present (Investigation B — stale feedback). §7 account-split removed (people-split kept; DB scrubbed). §8 unified deep-link (`?month`/`?from&to`/`?budgetId`/`?debtId`) wired from budgets, debts, dashboard. tsc + 147 tests + build + boot green. |
| 2026-06-11 | [Consumer](react/CHANGELOG.md#v901--ux-improvements-debt-count-auto-approve-pagination-voice-to-text-2026-06-11) | **v9.0.1** | **UX improvements: debt count, auto-approve, pagination, voice-to-text.** Five targeted fixes: (1) right-aligned active debt count in filter tab row; (2) `autoConfirm` default flipped to `true` for new recurring schedules (4 files); (3) Create Household button already correctly positioned (no-op); (4) activity trail paginated at 10/page with Previous/Next; (5) voice-to-text overhauled — interim results, continuous mode, error toasts, tap-to-stop, glow animation. No schema change. Typecheck clean. |
| 2026-06-11 | [Consumer](react/CHANGELOG.md#v900--transaction-forms--categories-rebuild-txn-redesign-2026-06-11) · [Database](supabase/migrations/20260608120000_v9_txn_redesign.sql) | **v9.0.0** | **Transaction forms & categories rebuild (txn-redesign).** Big-bang release behind `FEATURES.txnRedesign`; forward-only migration. D1 transfers/investments are a single row (both FKs, no category); D2 reconciliation & investment-value updates are an account-level `reconciliation_offset` + dated quiet-log, never a transaction (feeds net worth, never spend/income); type-scoped category enums; loan_emi SYSTEM_SPLIT (interest expense + principal transfer leg). Constraints added last; INV-9 before/after reconciliation passed on prod (spend/income identical, 0 violations, 3 issues logged). New §7 invariant suite (INV-1..9). 132 tests green; tsc + build + dev-boot clean. |
| 2026-06-10 | [Consumer](react/CHANGELOG.md#v891--budgets-toolbar-cleanup-remove-copy-suggest-icon-only-2026-06-10) | **v8.9.1** | **Budgets toolbar cleanup.** Removed the "Copy" button and its `copyPrevious()` / `copyBudgets` functionality from the Budgets page. The "Suggest" button is now icon-only (Sparkles icon with tooltip). No schema change; pure UI cleanup. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v890--recurring-schedules-household--user-scoped-cloud-synced-2026-06-07) · [Database](db/MIGRATIONS.md) | **v8.9.0** | **Recurring schedules → household + user scoped (cloud-synced).** Userback #7830371. New `recurring_schedules` table (migration [`20260607160000`](supabase/migrations/20260607160000_v89_recurring_schedules.sql)) — household RLS + `created_by` attribution + `updated_at` trigger + delta-sync index. Added `'recurring'` as a first-class adapter entity (mappers, list/upsert/remove, Hybrid sync); store loads/persists through the adapter (was localStorage-only) with a one-shot migration of legacy local data. Local-only mode preserved. Typecheck + build clean; 123/124. Clears the last deferred money-model item. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v881--phase-5-freeze-the-money-model-flags-code-cleanup-no-behaviour-change-2026-06-07) | **v8.8.1** | **Phase 5 — flag freeze (code cleanup, no behaviour change).** Removed the `moneyModel`/`budgetsV2`/`entryV2` flag objects; inlined the always-on behaviour (account model, Budgets v2, Entry v2 are now unconditional). `FEATURES` keeps only `onboarding`, `askVyact`, `savedViews` (the lone surviving preference, default hidden). Completes the Money-Model execution program (phases 1–5). Typecheck + build clean; 123/124. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v880--money-model-v2-goalstax-removed-two-number-dashboard-ask-vyact-v2--alpha-feedback-fixes-2026-06-07) · [Database](db/MIGRATIONS.md) | **v8.8.0** | **Money-Model v2 + alpha-feedback (4 phases consolidated).** **Goals & Tax removed as modules** (Pulse now 4 components; Goals route/page/modals/nav/engines + tax-nudge deleted). **Two-number dashboard** — Cash Flow (flow) + Net Worth (stock) hero + actionable Pulse guidance (A7/#7). **B1.5 scoped categories** enforced at the data layer (transfers can't carry spend/earn categories). **Budgets → monthly/annual → category hierarchy** (sub-category allocations removed; column dropped, migration [`20260607150000`](supabase/migrations/20260607150000_v8_drop_budget_allocations.sql)). **Suggest editable + Copy**; **Reports breakouts permanent** (R6); **Planner advice by household type** (#8); **Ask Vyact** chip-routing fix + typing-stream + voice input + new interpret intents. Userback fixes: onboarding ₹0-budgets, light-mode checkbox theming, stub-copy removal. Typecheck + build clean; 123/124. Deferred: recurring sync. Next: Phase 5 flag-freeze. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v871--enable-money-model-epic-1-budgets-v2-entry-v2-config-only-2026-06-07) | **v8.7.1** | **Config-only patch — features flipped ON.** Enabled `moneyModel.*` (Epic 1: account-on-every-txn, opening balances, reconciliation, ledger, scoped categories), `budgetsV2.*` (history, allocations, suggest), and `entryV2` shortForm (B4.2/B4.3). Left OFF: goals, goalsLens, taxNudge, showSavedViews. No code change. Typecheck + build clean; 126/127. ⚠ Needs a browser QA pass — these were previously flag-OFF and verified by tests only. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v870--money-model-budget-allocations-b23--form-reshape-b42b43-2026-06-07) · [Database](db/MIGRATIONS.md) | **v8.7.0** | **Money-Model B2.3 + B4.2/B4.3 (the last deferred items).** B2.3 budget allocations: `Budget.allocations` + additive `budgets.allocations` jsonb (migration [`20260607140000`](supabase/migrations/20260607140000_v8_budgets_allocations.sql)) + sub-limit editor in BudgetFormModal with allocated/unallocated + over-allocation warning (`rollupAllocations`). B4.2/B4.3 form reshape: Time/Recurring/Note collapse behind "+ More details" (`entryV2.shortForm`), time defaults to now. Goal real-account backing deferred (goals removed in v8.6.0). All flag-OFF by default; dev-server validated. Every actionable money-model item now built. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v860--remove-the-goal-concept--its-pulse-association-reversible-2026-06-07) | **v8.6.0** | **Goal concept removed (for now, reversible).** Master switch `FEATURES.goals.enabled=false`: drops Goal Progress from the Pulse Score (renormalises over Budgets/Savings/Trend/Debt), and gates every goal surface — nav, `/goals` route, Dashboard panel, Add-FAB + `g` shortcut, Ask Vyact chips, PulseGauge row. Page/modals/data preserved dormant. Golden file updated (only diff: pulse goals 40→0). Validated via dev-server boot (HTTP 200, all edited modules transform clean) + typecheck + 126/127 suite + build. |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v850--money-model-epics-2--3-ui-budgets-goals-lens-tax-nudge-2026-06-07) | **v8.5.0** | **Money-Model Epics 2 & 3 UI.** Budgets page: B2.4 "Suggest" budgets (editable proposal from recurring+debts+goals+history, A8) + B2.2 6-month budget-vs-actual timeline. Goals page: B3.1 goals-as-lens explainer + B3.2 derived tax nudge vs a real Tax Reserve account (no phantom balance, A5). All gated by `budgetsV2`/`goalsLens`/`taxNudge` (OFF = prior pages). Golden file + transfer invariant intact. Remaining: B2.3 allocations (needs sub-limit schema) + B4.2/B4.3 form reshape (OFF-by-default polish). |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v840--money-model-epic-1-b16-backfill--account-ledger-ui-2026-06-07) · [Database](db/MIGRATIONS.md) | **v8.4.0** | **Money-Model Epic 1 complete + gate-passed.** B1.6 account backfill applied to prod after a read-only **dry-run (R2)**: idempotent, amount-invariant — creates a system Cash account per household + repairs accountless txns; verified 0 orphans, 2 cash accounts, `global_net` unchanged (193592.90). Migration [`20260607130000`](supabase/migrations/20260607130000_v8_money_model_b16_account_backfill.sql). Account ledger UI in `pages/Accounts.tsx`: computed balance (B1.2), Fix-balance reconcile → dated adjustment (B1.3), expandable per-account ledger with running impact (B1.4) — all gated by `moneyModel.*`, OFF = v7.1.3 page. Golden file + transfer invariant green. Remaining: budget/goals/tax UIs (engines ready). |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v830--money-model-overhaul-engines-for-all-four-epics-2026-06-07) · [Database](db/MIGRATIONS.md) | **v8.3.0** | **Money-Model Overhaul — engines for all four epics (flags OFF).** Epic 1: `accounts.opening_balance` + provenance ([`20260607120000`](supabase/migrations/20260607120000_v8_money_model_account_opening_balance.sql)), `lib/accountBalance.ts` (balance = opening + credits − debits; reconciliation writes a dated Balance Adjustment, never overwrites; B1.1 cash-default enforcement); adjustments excluded from spend/income totals but still move balances. Epic 2: `lib/budgetIntel.ts` (copy / suggest from recurring+debts+goals / history / allocation roll-up — read-only, A8). Epic 3: `lib/goalsLens.ts` (virtual goals count tagged contributions, 0 to Net Worth; real-backed read the account) + `lib/taxNudge.ts` (derived nudge vs a real Tax Reserve account; no phantom balance). Epic 4: B4.1/B4.4 already shipped; B4.2/B4.3 staged. Golden file + transfer invariant green; 18 new engine tests. All behind OFF flags — remaining: the new screens + the B1.6 account_id backfill (dry-run, R2). |
| 2026-06-07 | [Consumer](react/CHANGELOG.md#v820--money-model-overhaul-flags-regression-safety-net-quick-wins-2026-06-07) | **v8.2.0** | **Money-Model Overhaul — phase 0.** First landing of the [money-model program](vyact-money-model-execution-and-regression.md), per its own discipline: program flags (`moneyModel`/`budgetsV2`/`goalsLens`/`taxNudge`/`entryV2`, Epic cores OFF = v8.1.2), the mandated regression safety net **before** Epic 1 (golden-file of the aggregation engine + transfer-invariant suite, Part C/C4), and the three extractable quick wins — B2.1 colour picker removed (deterministic `deterministicColor()`), B4.1 no keypad auto-launch, B4.4 Saved Views hidden by default (table/RPC kept dormant). Epic 1 data-model + migration core scaffolded behind OFF flags, gated on this net. No schema change. |
| 2026-06-06 | [Consumer](react/CHANGELOG.md#v812--realtime-refresh-race-currency-in-drawer-db-cleanup-2026-06-06) · [Database](db/MIGRATIONS.md) | **v8.1.2** | **Three triaged fixes.** (1) **Dashboard totals froze after a couple of txns** — realtime fired `refresh()` per postgres event with no debounce/ordering, so overlapping re-lists resolved out of order and a stale snapshot clobbered newer state. Fix: `refreshSeq` guard (discard stale/out-of-order completions) + 400ms debounce on the realtime handler. (2) **Currency change not reflected in the menu drawer** — `updateProfile` updated `profile.baseCurrency` (Dashboard/Reports reconvert fine) but not the `households` list the drawer renders; now patches the active `HouseholdMeta` in place. (3) **DB cleanup** — dropped the dead `households.baseline_provenance` column (legacy v8 onboarding draft residue) via [`20260606140000_v8_drop_legacy_baseline_provenance.sql`](supabase/migrations/20260606140000_v8_drop_legacy_baseline_provenance.sql) (drop+recreate `my_households` around it); applied to prod. |
| 2026-06-06 | [Consumer](react/CHANGELOG.md#v811--hotfix-my_households-400-blocked-app-load-2026-06-06) · [Database](db/MIGRATIONS.md) | **v8.1.1** | **App-blocking hotfix.** `GET my_households?select=…,onboarding` 400'd for all cloud users (failed `listHouseholds` on init). Cause: `my_households` froze its column list at creation, so the v8 `households.onboarding` column was never exposed by the view. Fix: migration [`20260606130000_v8_fix_my_households_view.sql`](supabase/migrations/20260606130000_v8_fix_my_households_view.sql) drops + recreates the view (re-expands `h.*`, restores `security_invoker`/grant; applied to prod), plus `supabaseAdapter.listHouseholds()` retries without `onboarding` on error so an additive column can never hard-block the app again. |
| 2026-06-06 | [Consumer](react/CHANGELOG.md#v810--ask-vyact-assistant-deterministic-no-llm-2026-06-06) | **v8.1.0** | **Ask Vyact assistant (deterministic, no-LLM).** Grows the existing launcher into a three-bucket assistant — **Capture / Interpret / Forecast** — via an on-device five-stage pipeline (`normalise → entityExtract → classifyIntent → resolve → phraseResponse`), behind `FEATURES.askVyact` (off → exact v7.4.5 launcher). New `lib/askVyactParser.ts` (stages 1–2, pure), `lib/askVyactResponses.ts` (stage 5, ≥3 warm variants/intent+outcome), `lib/askVyactBackend.ts` (`resolve` computes ONLY via existing services — `spendByCategory`/`liquidAssets`/Planner-style affordability·runway·goal·prescriptive; `RulesBackend` + stub `LlmBackend` + `proactiveInsight`); extended `lib/askVyactIntents.ts` with the free-text taxonomy + `classifyIntent`. Capture seeds the existing `TransactionFormModal`; Interpret figures match the dashboard and flag estimates (v8.0.1 provenance); Forecast always offers a constructive alternative, no securities/products. `classifyIntent` + `phraseResponse` are the two seams a future LLM swaps in with zero change to stages 1/2/4. Wired in `Chat.tsx`; all parsing on-device. 18 unit tests (`CON-UNIT-ASK-*`). Typecheck + build clean. No schema change. |
| 2026-06-06 | [Database](db/MIGRATIONS.md) | **v8-onboarding-state** | **v8 onboarding cloud persistence.** [`20260606120000_v8_onboarding_state.sql`](supabase/migrations/20260606120000_v8_onboarding_state.sql) adds `households.onboarding` (jsonb, object-guard, the per-household state machine record) and normalized provenance columns `confidence` / `source` / `estimated_at` / `confirmed_at` on `transactions`, `budgets`, `goals`, `debts`, `assets` (defaults `'confirmed'` / `'user'` → no backfill, existing data never re-tagged) plus a partial index per table on the still-outstanding estimates. Inherits existing RLS. Backs consumer v8.0.1. |
| 2026-06-06 | [Consumer](react/CHANGELOG.md#v801--onboarding-cloud-persistence-multi-device-2026-06-06) | **v8.0.1** | **Onboarding cloud persistence (multi-device).** Closes the v8.0.0 gap where onboarding state was browser-local (lost on a second device / cache clear). **(a)** Per-household state now write-throughs to `households.onboarding` (jsonb) with a localStorage cache for offline/local mode — `registerOnboardingSync` / `hydrateOnboardingFromCloud` in `onboardingState.ts`, mapped in `supabaseAdapter` (`listHouseholds` + `updateHousehold`), wired in `store.init()`. **(b)** Record provenance moved from a local overlay to normalized `confidence`/`source`/`estimated_at`/`confirmed_at` columns on the baseline-derived entity tables, so it rides the existing entity sync + RLS, survives a cache clear, and is SQL-queryable; `WithProvenance` mixed into the entity types, seeded directly in `Onboarding.tsx`, nudge stats derived from store entities. Requires migration `20260606120000_v8_onboarding_state.sql`. Typecheck + build clean; 16/16 onboarding tests green. |
| 2026-06-06 | [Consumer](react/CHANGELOG.md#v800--onboarding-activation-module-2026-06-06) | **v8.0.0** | **Onboarding & Activation module (major).** Ships the per-household onboarding flow from [`vyact-onboarding-engineering-spec.md`](vyact-onboarding-engineering-spec.md), built behind a single feature flag (`src/config/features.ts` → `FEATURES.onboarding`) so `enabled = false` is a clean no-op. **State model:** new `lib/onboardingState.ts` — per-household machine (`not_started → in_progress → completed / skipped`) with resume, the **21-day** confirmation window, and a record-level provenance overlay (`estimated / confirming / confirmed` × `onboarding / user / bank`), persisted as a namespaced localStorage overlay (no Supabase schema change). Existing households are migrated to `skipped`, never re-onboarded, data never re-tagged. **Flow:** rewrote `pages/Onboarding.tsx` to the 6-step segment-driven spine (Welcome → Segment → Context → Snapshot → Forward Model → Reveal) with content from `lib/onboardingTemplates.ts` (individual / household / smb); mandatory segment, skippable from step 2, ≤6 inputs, **no bank/statement/account field anywhere**; seeds provenance-tagged estimates reusing existing entities. **Trigger:** `App.tsx` routes fresh households into the flow and is **auth-method-agnostic** — email and (future) Google OAuth sign-ups both flow through it; invited members of a completed household skip baseline capture. **Honest data:** shared `<EstimatedTag/>`. **Progressive capture:** `lib/onboardingNudges.ts` + `NudgeBanner` — one gentle nudge at a time (check-in d7/14/21 → confirm-estimate → bank-connect), max one/session, tapers after 21 days, bank-connect never at signup (≥5 logs, once). **Instrumentation:** new onboarding/nudge analytics events. **Tests:** 16 unit tests (`CON-UNIT-ONB-001..016`). Typecheck + build clean. |
| 2026-06-05 | [Consumer](react/CHANGELOG.md#v747--user-feedback-heatmap-analytics-hotjar-userback-2026-06-05) | **v7.4.7** | **User feedback & heatmap analytics integration.** **(1) Hotjar via ContentSquare** — heatmap tracking script (`https://t.contentsquare.net/uxa/...`) injected into `index.html` for behavioral session recording and scroll/click heatmaps. Runs only on production builds. **(2) Userback widget** — user feedback collection widget (`@userback/widget` npm package, v0.3.12) initialized in `main.tsx` with project key (`A-7Q0Mz7gfB3ECVu6ZsOIUew97E`). Appears as a floating action button for users to screenshot + annotate + submit feedback. Both integrations are **telemetry-safe**: none of the third-party collectors have access to user financial data (transactions, budgets, balances stored client-side in localStorage / cloud-encrypted). No schema change. Typecheck clean. |
| 2026-06-05 | [Consumer](react/CHANGELOG.md#v746--transactions-header-overlap-fix--one-click-update-refresh-2026-06-05) | **v7.4.6** | **Two surgical follow-ups to v7.4.5.** **(1) Transactions header overlap fix** — the `CalendarDays` toggle pill overlapped the page heading at narrow widths. Moved off the title row onto the existing toolbox row that hosts Saved Views; Calendar is now left-aligned, Views/Save View stay right-aligned, `flex-wrap` keeps it tidy on phones. **(2) One-click "Refresh to update"** — the v7.4.5 update banner often took 4–5 clicks because `window.location.reload()` returned precached HTML/JS while the SW kept serving the old build. New `forceReloadForUpdate()` helper in `lib/pwa.ts` calls `registration.update()`, messages `SKIP_WAITING` if a waiting worker appears, otherwise unregisters every service worker, purges Cache Storage, and hard-reloads with a cache-busting `_v=` query param so the document and all subresources hit the network. `UpdateBanner.tsx` shows a spinning icon + "Refreshing…" label and disables the button so a single click is decisive. Typecheck clean. |
| 2026-06-05 | [Consumer](react/CHANGELOG.md#v745--mobile-edge-swipe-header-polish-fab-layering-ios-install-suppression-ask-vyact-two-tap-2026-06-05) | **v7.4.5** | **Mobile edge-swipe + Ask Vyact two-tap + UX polish.** Six small upgrades. **(1) Edge-swipe gestures** — left-edge → right opens the mobile sidebar; left-swipe on the open drawer closes it. New `useEdgeSwipe()` / `useSwipeToClose()` hooks in `hooks.ts`, gated `< 1024px`. **(2) Dashboard "Add Transaction" button removed** — redundant with the global Add-FAB; header collapses to greeting + subtitle. **(3) FAB stack desktop spacing** — `FloatingTools` (Planner + Ask Vyact bubble) bumped from `lg:bottom-[88px]` to `lg:bottom-[160px]` so it stops overlapping the Add-FAB. **(4) Header normalisation** — Transactions / Recurring / Budgets headers aligned with Goals / Debts / Net Worth: removed `flex-wrap`, added `min-w-0` + `flex-shrink-0`, swapped the chunky `<Button>` for the canonical `btn-primary` raw button. Budgets view-toggle hidden on `< sm`. **(5) iOS install banner suppressed + TD-22 filed** — iOS Safari has no programmatic install path, so the banner is now a pure no-op on iOS (Android/desktop unchanged). The audit confirmed Settings → Push notifications is currently a stored preference only; filed **TD-22** with a 4-phase remediation plan (foundation → subscribe → delivery → settings UI). **(6) Ask Vyact two-tap quick actions** — the six hardcoded "Try asking" prompts are replaced with a 4-bucket launcher: **Capture** (Add expense / income / transfer / investment / goal / budget / debt / asset, expense and income expand to a tap-2 row of common categories that seed the global Add-Transaction modal), **Inquire** (Spend, Pulse, Net worth, Budgets at risk, Top categories, Upcoming bills), **Plan** (Emergency fund, Debt strategy, Goal ETA), **Manage** (open Budgets / Net Worth / Households). New `lib/askVyactIntents.ts` exports a typed `Intent[]` registry + `IntentAction` discriminated union; new `seedTxn` store slot + `openAddTxn(seed?)` thread the seed into `TransactionFormModal`. Free-text input still routes through `selectChatBackend()`. Telemetry: privacy-safe `console.debug('[ask-vyact-intent]', { id, taps })`. Typecheck clean. |
| 2026-06-04 | [Consumer](react/CHANGELOG.md#v744--settings-password-dashboard-navigation-txn-click-to-edit-multi-entry-add-number-system-fix-2026-06-04) | **v7.4.4** | **Settings password, dashboard navigation, txn click-to-edit, multi-entry Add, number-system fix.** Five-in-one release. **(1) Change-password panel in Settings** above MFA — new password, confirm, show-passwords toggle, Save. Reuses `updatePassword()`; gated on cloud mode. **(2) Dashboard cards now navigate** — whole-card click targets: Pulse → `/reports`, Total Balance → `/networth`, Monthly Income → `/transactions?type=income`, Monthly Expenses → `/transactions?type=expense`, Savings Rate → `/budgets`, Spending donut → `/reports`. Transactions page reads `?type=` / `?cat=` on mount and seeds filters. **(3) Recent-transaction rows on Dashboard are click-to-edit** — same modal as the Transactions page. **(4) Always-available `AddFab`** — 56px coral primary FAB pinned bottom-right, respects `env(safe-area-inset-bottom)`, auto-hides on scroll-down so it doesn't block content, returns on scroll-up, hidden on `/auth/*`. Long-press (touch) or right-click (desktop) opens a speed-dial with Add Goal / Budget / Debt / Asset; plain tap = Add Transaction. Global keyboard shortcuts `n`/`g`/`b`/`d`/`a` promoted from page-scoped to app-wide via `Layout.tsx`. Content area gains `pb-28` on mobile so the last list row is never hidden. **(5) Bug fix — Number System dropdown was unselectable.** Picking "Indian — K / L / Cr" snapped back to "Western" instantly because `supabaseAdapter.updateProfile` never mapped `numberSystem` to a DB column; the cloud round-trip returned `undefined` and overwrote the user's pick. New `numberSystemPref.ts` localStorage overlay (per-household) persists the choice and is layered on top of the cloud profile in `store.refresh()` and `updateProfile()`. Mirrors the documented "client-side overlay for un-migrated schema" pattern; no DB migration required. Typecheck clean. |
| 2026-06-04 | [Consumer](react/CHANGELOG.md#v743--transactions-slim-filter-bar-active-chips-result-count--net-2026-06-04) | **v7.4.3** | **Transactions: slim filter bar.** The Transactions page used to spend ~240px of mobile real estate on five full-width filter controls before the first row rendered. New layout: single search input with inline `Search` icon and clear button, plus a `SlidersHorizontal` icon-button (with active-count badge) that opens a popover holding the four selects (Type / Category / Month / Member) and a **Reset** action. Removable chip row underneath shows each active filter for one-tap clearing, with a **Clear all** link. Result count and filtered net (`42 transactions · Net −$1,284.50`) sit under the chips for instant filter-impact feedback. Click-outside and Escape dismiss the popover. Smarter empty state — with filters active, shows a focused message and a Clear filters button instead of the generic "No transactions found". `/` keyboard shortcut now focuses the search box. No schema change. Typecheck clean. |
| 2026-06-04 | [Consumer](react/CHANGELOG.md#v742--fix-sidebar-auto-close-body-scroll-lock-help-default-closed-2026-06-04) | **v7.4.2** | **UX bugfix patch.** (1) **Sidebar didn't auto-close on Settings / Help** on mobile — every grouped nav item in `Sidebar.tsx` called `onClose()` on click but the footer `NavLink`s for Settings and Help skipped the handler. Extracted a `closeOnMobile` helper and wired both. (2) **Background scrolled while interacting with the mobile sidebar** — swipe gestures inside the drawer chained to the page once the nav reached its scroll boundary. Added `document.body.style.overflow = 'hidden'` while the mobile drawer is open plus `overscroll-contain` on the inner nav. (3) **Help page opened its first accordion by default** — changed `useState<number\|null>(0)` to `null` so users land on a clean collapsed list. No schema change. Typecheck clean. |
| 2026-06-04 | [Consumer](react/CHANGELOG.md#v741--fix-forgot-password-loop--ios-install-banner-2026-06-04) | **v7.4.1** | **Bugfix patch.** (1) **Forgot password was an infinite loop** — SignIn linked to `/auth/reset` but AuthGate only listed `/auth/reset-password` in `PUBLIC_ROUTES`, so unauthenticated users got bounced to `/auth/sign-in?next=/auth/reset` and the reset page never loaded, blocking the email request. Added `/auth/reset` to both `PUBLIC_ROUTES` and `RECOVERY_ROUTES` and aligned the SignIn link to the canonical `/auth/reset-password`. (2) **iOS install banner showed a dead Install button** — only Android / desktop Chrome / Edge fire `beforeinstallprompt`; iOS Safari has never supported it. Banner now hides the Install button on iOS and shows Share → Add to Home Screen instead. Fixed iPadOS 13+ detection (was reporting as MacIntel — now checks `maxTouchPoints`) and added an explicit "Open in Safari to install" message for iOS Chrome / Firefox / Edge and in-app webviews (Facebook / Instagram / Line / Twitter / LinkedIn) which cannot install at all. New `isIosSafari()` helper in `pwa.ts`. No schema change, no new deps. Typecheck clean. |
| 2026-06-04 | [Consumer](react/CHANGELOG.md#v740--installable-pwa-service-worker-install-banner-ios-support-2026-06-04) | **v7.4.0** | **Installable PWA.** Vyact is now installable to the home screen on Android, desktop Chrome / Edge, and iOS Safari (Add to Home Screen). VitePWA-generated manifest (`id: /`, `display: standalone` with `display_override: window-controls-overlay,standalone,minimal-ui`, theme `#E26D5C`, three jump-list shortcuts). Workbox service worker with navigation fallback to `/index.html` (denylisting `/api/*`, `/auth/*`, `version.json`), runtime caches for Google Fonts (StaleWhileRevalidate / CacheFirst) and Supabase REST (NetworkFirst, 4 s timeout / 24 h max-age); `skipWaiting: false` so the existing `UpdateBanner` keeps owning the update UX. New `src/lib/pwa.ts` registers the SW (production only) and re-emits browser lifecycle as `vyact:installable / installed / sw-update`. New `InstallBanner` (mounted at app root) prompts via `beforeinstallprompt` on Android/Chromium and falls back to a Share → Add to Home Screen hint on iOS, dismissible per-session and capped at three lifetime dismissals. iOS PWA meta block in `index.html` (apple-mobile-web-app-capable, apple-touch-icon, apple-mobile-web-app-title, dual theme-color, viewport-fit=cover). `UpdateBanner` now also reacts to a waiting SW (calls `applyUpdate()` → skip-waiting + reload on `controlling`). Static `public/manifest.webmanifest` removed (VitePWA emits canonical). **Follow-ups:** drop PNG icons (192 / 512 / 512-maskable) into `react/public/icons/` for richer Android tiles & iOS — manifest currently references `favicon.svg` with `purpose: 'any maskable'` (Chrome / Edge accept SVG since 2022). Typecheck clean. |
| 2026-06-03 | [Consumer](react/CHANGELOG.md#v732--cosmetic-polish-dropdowns-row-icons-money-overflow-scroll-ux-2026-06-03) | **v7.3.2** | **Cosmetic polish patch.** Single canonical dropdown treatment app-wide via new `.ff-select` class (custom SVG chevron, rounded options, dark-theme variant) inherited by every native `<select class="input">` so Settings/Onboarding/Recurring filters now match the Splits-style in-modal selects. `.row-action` icon-button standard rolled out to Recurring + NetWorth rows (Lucide `Pencil`/`Trash2`/`ToggleLeft`); Settings → Sync & Backup buttons re-shaped as `btn-secondary` tiles with Lucide icons. New `ScrollToTop` component resets scroll on every route change. Money overflow on phones fixed across Dashboard / Budgets / Reports / Splits / NetWorth: tightened `<Money maxChars>` thresholds, responsive Card font scale, `min-w-0` flex containers, Budgets summary stacks on `<sm`. `.num` redefined to JetBrains Mono so every row-level amount matches the Reports → Period Summary mono treatment. No schema change. Typecheck clean; build clean. |
| 2026-06-03 | [Consumer](react/CHANGELOG.md#v731--patch-release-version-alignment--ui-polish-2026-06-03) | **v7.3.1** | **Patch release: version alignment & UI polish.** Advances the consumer line by one patch and aligns the runtime package/docs version stamps. Also records the small fixes already shipped in this patch window: text-based transaction time entry, Settings-only placement for sensitive export/reset actions, and sidebar label cleanup for Accounts / Insights. Typecheck clean; no schema change. |
| 2026-06-03 | [Consumer](react/CHANGELOG.md#v730--saved-views-account-drawer--education-progress-2026-06-03) | **v7.3.0** | **Saved Views, Account Drawer & Education progress.** Lands the v7.2.0-rc deferred trio. New `saved_views` table + RLS + `replace_saved_views` RPC ([`20260602140000_money_map_saved_views.sql`](supabase/migrations/20260602140000_money_map_saved_views.sql)) with per-row `is_shared` opt-in for household sharing; wired through `DataAdapter` (`'savedViews'` entity), `supabaseAdapter` (`tableName(entity)` helper, mappers, `replace_saved_views` RPC branch), and the Zustand store. New `SavedViewsBar` component mounted on Transactions, Reports, and Insights — strips private filter keys (`search`, `description`, `memberId`, txn ids) before persisting. New `AccountDrawer` (Money Map Item #5, the M-tier multi-account split) wired into `TransactionFormModal`: collapsible drawer with running sum, balanced indicator (±0.01), apply-remaining action, persists onto `transactions.extras.accountSplits` (table reshape queued for v7.4). Education progress wired end-to-end: `profiles.education_progress jsonb` flows through `getProfile`/`updateProfile` and a new `markEducation` store action; `WhyChip` primitive auto-marks `completed_at` on open and `dismissed_at` on close, with a 50-key LRU `localStorage` map for local-only mode. Reports breakouts switch to the pre-aggregated `v_txn_by_member` / `v_txn_by_account` cloud views when Money Map is `'on'` (mobile payload reduction, Item #8 second half) with transparent fall-back to client-side fold for shadow / off / local. Typecheck clean. |
| 2026-06-02 | [Consumer](react/CHANGELOG.md#v720-rc--money-map-dual-write-debt-direction--reports-breakouts-2026-06-02) | **v7.2.0-rc** | **Money Map dual-write default + Debt direction + Reports breakouts (RC).** Closes the v7.1.x Money Map track on the consumer side. New `replace_accounts` SECURITY DEFINER RPC ([`20260602130000_money_map_replace_accounts_rpc.sql`](supabase/migrations/20260602130000_money_map_replace_accounts_rpc.sql)) — completes the TD-09 set so `HybridAdapter.replaceAll('accounts', …)` round-trips to Supabase. `getMoneyMapMode()` now defaults to `'shadow'` automatically when `VITE_SUPABASE_URL` is set (cloud builds opt themselves in; local-only stays `'off'`). Debts page gains flag-gated `All / Owe / Owed to me` direction tabs; `totalLiabilities` and `totalMonthlyDebtPayment` now exclude receivables; new `totalReceivables` aggregator. Net Worth renders `Assets + Owed-to-me − Liabilities` whenever receivables exist and the Liabilities column splits into a denim-accented "Owed to me" sub-list. Reports gains flag-gated `By member` and `By account` panels (local fold over `reportableTxns`; `v_txn_by_*` server views remain a future read-path optimisation). **Status: release-candidate** — mode stays `'shadow'` (legacy reads, dual writes) until v7.2.1 promotes to `'on'`. **Deferred:** account-drawer M-tier UI (multi-account split), saved views entity slice (slated for v7.3). Typecheck clean. |
| 2026-06-02 | [Consumer](react/CHANGELOG.md#v713--accounts-management-ui-2026-06-02) | **v7.1.3** | **Accounts management UI.** Flag-gated **Accounts** page (`vt_feature_money_map !== 'off'`) lists every account with add / rename / archive / set-default flows; replaces the v7.1.2 "synthesised from assets" read-only view with first-class CRUD on the `accounts` table. Accounts modal mounted at App root via the v6.4 store-slot pattern. |
| 2026-06-02 | [Consumer](react/CHANGELOG.md#v712--accounts-as-a-first-class-entity-2026-06-02) | **v7.1.2** | **Accounts as a first-class entity.** Adds `accounts` to the data adapter, the `HybridAdapter` per-entity sync sentinel, and the Zustand store. Synthesises from `assets` in local-only mode (back-compat). Powers the v7.1.3 Accounts page. |
| 2026-06-02 | [Consumer](react/CHANGELOG.md#v711--money-map-dual-write-cloud-adapter-2026-06-02) | **v7.1.1** | **Money Map dual-write (cloud adapter).** `SupabaseAdapter` writes both the new FK columns (`account_id`, `to_account_id`, `initiated_by`, `direction`, `counterparty_name`) and the legacy `extras.linkedAssetId` whenever the flag is `'shadow'` or `'on'`. Reads prefer FK with legacy fallback. Guarantees a v7.1 and v7.2 client viewing the same household see the same data during the dual-write window. |
| 2026-06-02 | [Consumer](react/CHANGELOG.md#v710-rc--money-map-foundation-2026-06-02) | **v7.1.0-rc** | **Money Map foundation.** Lands `vt_feature_money_map` flag (`'off'|'shadow'|'on'`), `Account` / `SavedView` types, and the v7.2 [`docs/SOLUTION_MONEY_MAP.md`](docs/SOLUTION_MONEY_MAP.md) spec. No user-visible UI yet — v7.1.1 wires the writes, v7.1.2/.3 land the entity + UI, v7.2.0-rc completes the surface. |
| 2026-06-02 | [Database](db/MIGRATIONS.md) | **money-map-phase1-accounts** | **Money Map Phase 1 backend (PR alongside consumer v7.1.x).** [`20260602120000_money_map_phase1_accounts.sql`](supabase/migrations/20260602120000_money_map_phase1_accounts.sql) creates the `accounts` table (with `accounts_default_per_currency` partial unique index), adds `transactions.{account_id, to_account_id, initiated_by}` FKs, adds `debts.{direction, counterparty_name}` with the `owed_by_me|owed_to_me` check, ships the `v_txn_by_member` / `v_txn_by_account` aggregate views, and runs the idempotent backfill from `assets` -> `accounts`. RLS mirrors the existing `transactions` policies. Companion RPC [`20260602130000_money_map_replace_accounts_rpc.sql`](supabase/migrations/20260602130000_money_map_replace_accounts_rpc.sql) ships in v7.2.0-rc. |
| 2026-06-02 | [Consumer](react/CHANGELOG.md#v703--track-specific-transaction-modal-2026-06-02) | **v7.0.3** | **Track-specific transaction modal (flag-gated).** Four-card track picker (Spend / Income / Transfer / Investment) with per-track field gating and per-track category lists. Transfers expand into linked expense+income rows tagged `__tg:<gid>` so reports self-cancel. New `linkedToAssetId` rides on the existing `extras` JSON — zero schema migration. New `INVESTMENT_CATEGORIES`, `CATEGORIES_BY_TYPE`, `featureFlags.ts` (`vt_feature_track_picker`, default OFF). |
| 2026-06-01 | [Consumer](react/CHANGELOG.md#v702--dashboard-cold-start-fix--concrete-recent-activity--ff-hard-cut-2026-06-01) | **v7.0.2** | **Dashboard cold-start fix + concrete Recent Activity + `ff_` hard-cut.** `HybridAdapter` now awaits the first cloud sync per (household, entity) so the dashboard isn't blank on first post-login render; Recent Activity in Households shows real lines like "Maya logged Groceries · $84.20" with column-level diffs, entity filters, and an actor dropdown; legacy `ff_` localStorage writes are removed (reads still honour legacy keys); adds `OPERATIONS.md`. |
| 2026-06-01 | [Consumer](react/CHANGELOG.md#v701--archive-vanilla-shell--google-button-coming-soon-2026-06-01) | **v7.0.1** | **Archive vanilla shell + Google button (coming soon).** Removes v5 vanilla shell files. Google sign-in button promoted to primary CTA on Sign In + Sign Up with "coming soon" toast until provider is configured. |
| 2026-06-01 | Consumer (infra) | **v7.0.1 (prev entry)** | **Archive vanilla shell.** Removed `index.html`, `app.js`, `style.css`, `src/dataAdapter.js`, `setup.sh`, `vercel-setup.sh`, `QUICKFIX.md`, and `DEPLOYMENT.md` from the working tree. All pages are already ported to React; `vercel.json` already builds from `react/` only. Source preserved in git history. |
| 2026-06-01 | [Consumer](react/CHANGELOG.md#v700--rebrand-finflow--vyact-2026-06-01) | **v7.0.0** | **Rebrand: FinFlow → Vyact.** Full consumer app rename — titles, meta, PWA manifest, localStorage keys (`vt_` prefix with `ff_` compat shim), export filenames, env URLs, X-Client-Info header. Repo renamed to Vyact on GitHub. Historical changelog entries preserved. No schema changes; no data migration required. |
| 2026-06-01 | [Admin](admin/CHANGELOG.md) | **v1.1.0** | **Admin rebrand to Vyact.** Titles, meta, supabase client header updated. |
| 2026-05-30 | [Consumer](react/CHANGELOG.md#v660--earnable-pulse-score--google-sign-in--reset-without-email-2026-05-30) | **v6.6.0** | **Earnable Pulse Score + Google sign-in + reset-without-email.** Pulse Score (plan 09) drops the arbitrary component defaults that pinned empty accounts at ~55: each component is scored only when it has data and the weights renormalise, so an empty account reads `null` → gauge shows "— / No data yet" instead of a fake number; `PulseGauge` respects `prefers-reduced-motion`. Adds a `GoogleButton` (plan 11, gated on `isCloudEnabled`) to Sign In / Sign Up / Reset — needs the Supabase Google provider configured before it works (dashboard step, not in this code release). Password reset (plan 07) gains Google + magic-link fallbacks + a no-cloud state so it never dead-ends without SMTP. `aiSummary.ts` widened to accept `pulseScore.total: number\|null`. Lint clean, build green; no schema change (plan-10 migration ships separately). |
| 2026-05-30 | [Consumer](react/CHANGELOG.md#v651--in-app-new-version-available-update-prompt-2026-05-30) | **v6.5.1** | **In-app "new version available" update prompt.** Fixes deploys not reaching long-lived tabs (the v6.5.0 footer-stuck confusion). Build stamps `dist/version.json`; a new `UpdateBanner` polls it (`no-store`, on load/focus/15-min) and prompts Refresh when the deployed version differs from the running `__APP_VERSION__`. No service worker involved; purely additive UI. |
| 2026-05-30 | [Consumer](react/CHANGELOG.md#v650--e2e-test-automation-foundation-batch-12--test-build-cloud-leak-fix-2026-05-30) | **v6.5.0** | **E2E test automation foundation + test-build cloud-leak fix (non-functional release).** No runtime/UI change. Lands the Playwright E2E suite (20 passed / 3 skipped on chromium) covering Transaction Creation, Budgets, and Debt payment math, with Page Objects, deterministic fixtures, and the test-case inventory (`react/e2e/TEST_CASE_INVENTORY.md`, 20/163 developed) as the QA source of truth. Adds a11y groundwork that also stabilises test locators (`Modal` `role="dialog"`/`aria-labelledby`, `Field` `htmlFor`/`id`). Build fix: the v6.4.27 production Supabase `FALLBACK_URL/KEY` was applied on the e2e `--mode test` build too, flipping the test app into cloud mode and gating every route behind `/auth/sign-in`; the fallback is now skipped when `MODE === 'test'` — real production (`MODE === 'production'`) is unchanged. |
| 2026-05-30 | [Consumer](react/CHANGELOG.md#v6427--production-db-connection-fix--deploy-hardening--in-app-version-note-2026-05-30) | **v6.4.27** | **Production DB-connection fix + deploy hardening + in-app version note.** Root-caused why the live consumer showed *dummy/seeded* data: the CI build shipped in local-only mode because `VITE_SUPABASE_URL`/`ANON_KEY` never reached it. Fixed by committing `react/.env.production` + `admin/.env.production` with the **public** project URL + publishable key (RLS enforces security), and removing the empty-secret `VITE_SUPABASE_*` injection from `deploy.yml` (Vite gives shell env vars precedence over `.env` files, so an empty secret silently overrode the config). Deploy pipeline now deterministic & self-contained: every push to `main` deploys; `db-migrations` is best-effort and never blocks the app deploys. Added a build-time version sub-note (from `package.json`) to the **Help & Guide** page. Corrected the documented live URLs to the real Vercel production domains. Admin verified healthy (NorthStar dashboard on live prod data). See [`DEPLOY.md`](DEPLOY.md). |
| 2026-05-29 | [Consumer](react/CHANGELOG.md#v6426--money-typography-standardisation-one-canonical-figure-style-across-all-sections-2026-05-29) | **v6.4.26** | **Money typography standardisation.** One canonical `.num` figure style (non-italic Inter Tight, tabular + lining numerals) for every amount across Dashboard, Budgets, Splits, Transactions, Net Worth, Debts, Reports — replacing the previous mix of italic-serif KPI/hero numbers, mono transaction rows, and plain non-tabular sans totals. `<Money>` now applies `.num` automatically; headings stay editorial italic; dense numeric tables stay mono. Purely visual — no value/format change. |
| 2026-05-29 | [Database](db/MIGRATIONS.md) | **td-08-09-13-honest-residuals** | **TD-08 / TD-09 / TD-13 actually in prod now (PR #20).** Four migrations: `20260529150000_td13_budgets_add_period.sql` (adds the period columns the consumer's been faking with `budgetMeta.ts` since v6.4), `20260529150500_td08_audit_triggers.sql` (`log_domain_activity` SECURITY DEFINER + 6 triggers, with the `search_path` + memberships corrections from the review), `20260529151000_td09_replace_all_rpcs.sql` (6 `replace_<entity>` SECURITY DEFINER RPCs), `20260529151500_td08_td09_harden_execute_grants.sql` (revokes the default PUBLIC execute grant after the security advisor flagged anon-exposure of the new functions). **Applied to prod directly via the Supabase MCP `execute_sql` flow** (the GitHub Actions browser-merge path was unavailable), with the four `schema_migrations` tracker rows recorded to match the repo filenames 1:1 — so the CI `db push` on this release is a clean no-op. Discovery during prep: the earlier "Resolved on paper" claim for TD-13 was incorrect (baseline grep showed no `period` columns); fixes the `db/migrations-superseded/README.md` claim too. Security advisor re-checked post-apply: zero anon-executable and zero `function_search_path_mutable` warnings on any PR #20 function. |
| 2026-05-28 | [Database](db/MIGRATIONS.md) | **production-baseline** | **TD-20 reconciliation — closes TD-20 (PR #16).** Captures actual production schema as a single new baseline (`00000000000001_production_state_baseline.sql`, ~46 KB: 16 tables, 18 indexes, ~55 RLS policies, 15 functions incl. the live admin RPCs, 12 triggers). The earlier 8 design-intent migrations move to `db/migrations-superseded/` (out of the apply path, kept for git history). New `db-migrations` job in `deploy.yml` runs `supabase db push --include-all` before the app deploy jobs so the apply step is no longer manual. Baseline pre-recorded in `supabase_migrations.schema_migrations` so the first deploy is a verified no-op against prod. Three follow-up hotfixes shipped on the same day: PR #17 pinned Supabase CLI version (rate-limit fix), PR #18 added minimal `supabase/config.toml` (required by `supabase link`), PR #19 added 13 stub files matching the pre-baseline prod-tracker entries (CLI requires 1:1 sync between local files and remote tracker). |
| 2026-05-24 | [Consumer](react/CHANGELOG.md#v6425--lead-review-pass--td-08-audit-triggers--td-04-extension-catch-up-remediation-pr-13-batch-2026-05-24) | **v6.4.25** | **Lead review pass over the dev's PR #13 batch — closes TD-08/09/10/12/13/15.** Adds TD-08 audit triggers (with the missing `memberships` + `search_path` corrections the dev's patch lacked); accepts TD-04-ext-a/b; documents the TD-04-ext-c scope deviation (dev wrote subscription/content mutation RPCs instead of the requested admin_list_users/weekly_trend/ai_usage_summary). Filename-case + e2e ID + schema-regen blockers all corrected. |
| 2026-05-24 | db-migrations | audit-triggers | TD-08: server-side `activity_log` triggers on every domain table (incl. memberships, added during review) via `log_domain_activity()` SECURITY DEFINER function with `search_path` lockdown. |
| 2026-05-24 | db-migrations | content-items | TD-04-ext-b: `content_items` + `content_favorites` tables + RLS; wires `publishedArticles` / `contentFavorites` into `admin_dashboard_kpis()`. |
| 2026-05-24 | [Admin](admin/CHANGELOG.md#v108--slug-sanitiser-edge-case-fix-remediation-pr-3-2026-05-24) | **v1.0.8** | Slug sanitiser edge-case fix: `slugify()` now returns an empty string for punctuation-only inputs (ADM-UNIT-006). |
| 2026-05-24 | [Consumer](react/CHANGELOG.md#v6420--td-15-mfa-enrolment-auth-hardening) | **v6.4.20** | TD-15: Add MFA enrolment helpers and a Security subsection in Settings; docs/AUTH_HARDENING.md added. |
| 2026-05-24 | [Consumer](react/CHANGELOG.md#v6421--td-13-budgets-period-column-migration) | **v6.4.21** | TD-13: Add `period`/`period_start`/`period_end` columns to `budgets` (migration added). |
| 2026-05-24 | [Consumer](react/CHANGELOG.md#v6422--td-09-atomic-replace_all-rpc--adapter-call) | **v6.4.22** | TD-09: Add server-side `replace_<entity>` RPCs and call them from the Supabase adapter for atomic bulk replace operations. |
| 2026-05-24 | [Consumer](react/CHANGELOG.md#v6423--td-10-sync-status-badge--sidebar-mount) | **v6.4.23** | TD-10: Add a sidebar `SyncStatusBadge` to surface sync/queued/conflict state. |
| 2026-05-24 | [Consumer](react/CHANGELOG.md#v6424--td-12-memoised-selectors--dashboard-updates) | **v6.4.24** | TD-12: Add memoized selectors and update Dashboard to use them. |
| 2026-05-24 | db-migrations | subscriptions | Add `subscriptions` table and wire `paidSubscriptions`/`mrr` into `admin_dashboard_kpis()` (TD-04-ext-a). |
| 2026-05-24 | db-migrations | admin-rpcs | Add admin RPCs for subscriptions & content (admin_list_subscriptions, admin_cancel_subscription, admin_publish_content_item) (TD-04-ext-c). |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6419--td-03-phase-b-concurrency-wired-across-all-crud--in-app-conflict-banner-remediation-pr-12-2026-05-23) | **v6.4.19** | **TD-03 phase B: concurrency wired across all CRUD + in-app conflict banner (remediation PR #12). Closes TD-03.** Budget/Goal/Debt/Asset interfaces gain `updated_at?`; row mappers thread it from the cloud; `upsertBudget/Goal/Debt/Asset` store actions pass it as the precondition on edits. New `SyncConflictBanner` mounted in Layout polls `pendingConflictCount()` every 5s and shows a banner with a Dismiss action when conflicts dead-letter. `HybridAdapter.clearConflicts()` added. Two household members editing the same row no longer silently overwrite each other anywhere in the app. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6418--td-03-phase-a-optimistic-concurrency-at-the-cloud-boundary-remediation-pr-11-2026-05-23) | **v6.4.18** | **TD-03 phase A: optimistic concurrency at the cloud boundary (remediation PR #11).** SupabaseAdapter.upsert gains an optional `expectedUpdatedAt` precondition; when supplied, performs a guarded UPDATE and throws `ConcurrencyConflictError` if the cloud row has been touched since the caller's read. HybridAdapter dead-letters conflicts to `ff_sync_conflicts` and exposes `pendingConflictCount()`. Transactions edit is the first wired call site. 3 new tests (`CON-UNIT-051..053`). UI toast + the other 4 CRUD entities queued for PR #12. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6417--td-01-phases-cd-decimal-money--amortisation--cloud-boundary-remediation-pr-10-2026-05-23) | **v6.4.17** | **TD-01 phases C+D: decimal money — amortisation + cloud boundary (remediation PR #10).** **Closes TD-01.** Phase C: `amortization.ts` carries outstanding balance as Dinero across 300-row schedules; `splitPayment(…, currency)` quantises both interest and principal. Phase D: new `parseMoneyFromCloud` helper centralises the supabaseAdapter row-mapper boundary; `types.ts` gains the TD-01 discipline doc block. 4 new pins (CON-UNIT-047/048/049/050); 5 existing tests tightened. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6416--td-01-phase-b-decimal-money--aggregations-in-dinero-space-remediation-pr-9-2026-05-23) | **v6.4.16** | **TD-01 phase B: decimal money — aggregations in dinero space (remediation PR #9).** Every aggregator in `calculations.ts` (sums, grouped reduces, splits) folds in dinero integer-cents arithmetic; reductions no longer drift across `+`. `calculations.test.ts` tightened from `toBeCloseTo` to strict `.toBe` where exactness is now achievable. New `CON-UNIT-046` pins the float-drift fix on 10×$0.10 sums. Phases C/D still queued. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6415--td-01-phase-a-decimal-money--dineroJs-at-the-fx-boundary-remediation-pr-8-2026-05-23) | **v6.4.15** | **TD-01 phase A: decimal money — dinero.js at the FX boundary (remediation PR #8).** `convert()` now routes through dinero.js v2 with banker's rounding at the FX edge. Public signature unchanged; round-trip USD↔EUR is now exact (`CON-UNIT-006` flipped from characterization to positive assertion). 6 new money-module tests (`CON-UNIT-040..045`). Phases B/C/D still queued. |
| 2026-05-23 | [Database](db/MIGRATIONS.md) | **admin-roles-and-kpis** | **Admin privilege surface into migrations (remediation PR #7 / TD-04).** New migration `20260523060000_admin_roles_and_dashboard_kpis.sql` adds the `admin_role` enum, `admin_roles` table + RLS, `is_admin()` / `has_admin_role()` helpers, and the `admin_dashboard_kpis()` RPC. The privileged authorisation layer is no longer hand-run-only. `content_items` / `subscriptions` backed KPI fields return 0 until follow-up TD-04-extension migrations land them. |
| 2026-05-23 | [Database](db/MIGRATIONS.md) | **migrations-init** | **DB migrations toolchain (remediation PR #6 / TD-18).** New `supabase/migrations/` directory is the source of truth; current schema is `00000000000000_initial_schema.sql`. `db/schema.sql` is now a generated snapshot kept in sync by a new `db-migrations` CI gate. Foundation for PR #7 (TD-04 admin schema). |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6414--route-level-code-splitting-remediation-pr-5-2026-05-23) | **v6.4.14** | **Route-level code splitting (remediation PR #5 / TD-11).** Every page in `App.tsx` is now `React.lazy`-imported and wrapped in `<Suspense>`; Recharts ships only in the chunks for routes that import it (Dashboard / Reports / NetWorth). New `CON-E2E-006` regression spec verifies Recharts is not fetched on `/transactions`. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6413--top-level-error-boundary-remediation-pr-4-2026-05-23) | **v6.4.13** | **Top-level error boundary (remediation PR #4 / TD-05).** Adds a global `<ErrorBoundary>` to catch uncaught render errors and show a fallback UI with a reset button. New `CON-E2E-005` regression spec navigates to a throwing page and asserts the fallback. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6412--transactions-list-virtualization-remediation-pr-3-2026-05-23) | **v6.4.12** | **Transactions list virtualization (remediation PR #3 / TD-17).** Virtualizes the Transactions list with `@tanstack/react-virtual` for O(viewport) DOM nodes even at 10 000+ rows. `data-testid="txn-row"` added to support the acceptance check. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6411--test-scenarios-master-catalog--per-scenario-audit-evidence-remediation-pr-2-2026-05-23) | **v6.4.11** | **Test Scenarios master catalog + per-scenario audit evidence (remediation PR #2).** Every consumer test now carries a stable TS ID (`CON-UNIT-001..039`, `CON-E2E-001..004`) catalogued in `docs/TEST_SCENARIOS.md`. A reconciler gate refuses code↔doc drift. The automation report now captures per-app pass/fail counts, full failure details, and a complete pass register for audit on every run. |
| 2026-05-23 | [Admin](admin/CHANGELOG.md#v107--first-admin-unit-tests--test-scenarios-catalog-remediation-pr-2-2026-05-23) | **v1.0.7** | **First admin unit tests + test scenarios catalog (remediation PR #2).** Closes the privileged-admin-app safety-net gap (N1). 11 ID-tagged unit tests (`ADM-UNIT-001..011`) covering `slugify` + `rowToArticle`; `admin-unit` now part of the gate. Caught one real slugify edge-case bug for follow-up. |
| 2026-05-23 | [Consumer](react/CHANGELOG.md#v6410--eslint-floor-remediation-pr-1-2026-05-23) | **v6.4.10** | **ESLint floor (remediation PR #1).** First real linter; `npm run lint` now runs `eslint .`, `tsc --noEmit` preserved as `npm run typecheck`. Surfaces existing `exhaustive-deps` / unused-vars / `no-explicit-any` debt as warnings to be ratcheted by later TECH_DEBT PRs. One real bug fixed: short-circuit-as-statement in `Households.tsx`. |
| 2026-05-23 | [Admin](admin/CHANGELOG.md#v106--eslint-floor-remediation-pr-1-2026-05-23) | **v1.0.6** | **ESLint floor (remediation PR #1).** Same lint floor for the privileged admin app (no tests + no linter was the highest-risk gap). Source passes lint clean today (1 pre-existing warning). |
| 2026-05-22 | [Admin](admin/CHANGELOG.md#v105--mobile-navigation-fix-2026-05-22) | **v1.0.5** | **Mobile navigation fix.** Sidebar was desktop-only with no fallback; added a hamburger + slide-out nav drawer so tabs work below 1024px. |
| 2026-05-22 | [Consumer](react/CHANGELOG.md#v649--calendar-all-months-recurring-projection-day-filter-on-demand-2026-05-22) | **v6.4.9** | **Calendar overhaul.** Navigable across all months, projects upcoming recurring payments (denim), tap a day to filter the list to that date, and it's now toggled on-demand via a Calendar icon. |
| 2026-05-22 | [Consumer](react/CHANGELOG.md#v648--auto-even-split-shares-2026-05-22) | **v6.4.8** | **Auto-even split shares.** Bill splits default to an even split and rebalance as people are added; users only type a number to override. |
| 2026-05-22 | [Consumer](react/CHANGELOG.md#v647--calendar-view-on-transactions-page-2026-05-22) | **v6.4.7** | **Calendar view on Transactions page.** Visualizes which days of the month had expenses logged vs missed. |
| 2026-05-21 | [Admin](admin/CHANGELOG.md#v104--ai-assistant-intelligence-2026-05-21) | **v1.0.4** | **AI Assistant Intelligence** page — intent/sentiment distribution + per-user business segments from privacy-safe Ask-FinFlow usage (`admin_ai_usage_summary()` RPC). |
| 2026-05-21 | [Consumer](react/CHANGELOG.md#v646--linked-accounts-dynamic-needswants-ai-usage-metrics-split-bill-ux-2026-05-21) | **v6.4.6** | **Linked accounts + dynamic needs/wants + AI usage metrics.** Payments now draw from Net Worth accounts (cash/bank/credit-card); needs/wants moved to an admin-editable DB table; Ask-FinFlow logs privacy-safe intent/sentiment. Plus split-bill UX and a build-stabilisation pass (two non-compiling files rebuilt). |
| 2026-05-21 | [Consumer](react/CHANGELOG.md#v645--help-page-real-screenshots--interactive-gifs-2026-05-21) | **v6.4.5** | **Help page: real screenshots & GIFs.** Consolidated Help to 8 searchable topics, each backed by a real capture of the live app — 6 WEBP screenshots + 2 animated GIFs (Add Transaction, Split a bill). Added a reproducible `puppeteer-core` capture pipeline (`react/scripts/capture-help.mjs`). |
| 2026-05-21 | [Consumer](react/CHANGELOG.md#v644--finflow-design-system-v2-alignment-2026-05-21) | **v6.4.4** | **Design System v2 alignment.** Adopted the full `--ff-*` token set, matched buttons/inputs/cards to the `lib.jsx` FF specs, and upgraded the Pip mascot (eyes/cheeks/smile) + "Fin*Flow*" wordmark. Visual-only. |
| 2026-05-21 | [Admin](admin/CHANGELOG.md#v103--design-system-v2-brand-marks-2026-05-21) | **v1.0.3** | Design System v2 brand marks: full Pip mascot + Fin*Flow* wordmark + favicon. (Dark "shell" theme deferred pending sign-off.) |
| 2026-05-20 | [Consumer](react/CHANGELOG.md#v643--split-transaction-creation--buttoninput-styling-2026-05-20) | **v6.4.3** | Split-transaction creation restored in the Add/Edit modal (participants, shares, who-paid, validation) — persists full `SplitInfo` to Supabase. Fixed undefined `btn-*`/`input` classes that made ported-page buttons render as plain text. |
| 2026-05-20 | [Admin](admin/CHANGELOG.md#v102--brand-icon-fix-2026-05-20) | **v1.0.2** | Brand icon fix: added missing `admin/public/favicon.svg` (FinFlow Pip mark) and replaced the "FF" monogram in the sidebar with the logo-mark. |
| 2026-05-20 | [Consumer](react/CHANGELOG.md#v642--critical-sync-fix-cloud-writes-never-persisted-2026-05-20) | **v6.4.2** | **Critical sync fix.** Cloud writes never persisted: non-UUID ids (22P02) + UPDATE-instead-of-INSERT left every locally-created record stuck in the queue. Fixed `uid()`→`crypto.randomUUID()`, adapter→real upsert, + queue guard for legacy poisoned ops. |
| 2026-05-20 | [Admin](admin/CHANGELOG.md#v101--authgate-deadlock-hotfix-2026-05-20) | **v1.0.1** | **AuthGate deadlock hotfix.** App hung on "Checking session…" post-pause: an async `onAuthStateChange` callback awaited a nested `getUser()` and deadlocked the GoTrue auth lock. Deferred role resolution + pass session user id. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v641--sidebar-polish--debt--asset-form-parity-2026-05-10) | **v6.4.1** | Sidebar logo links to Dashboard. Budgets moved to PLAN, Splits moved to TRACK. Debt and Asset forms refactored to modals matching the Add Transaction / Budget / Goal pattern. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v64--blocker-sweep-persistence-form-parity-budget-periods-floating-tools-2026-05-10) | **v6.4** | 7-issue blocker sweep: cache no-clobber + sentinel persistence; Goal/Budget modal parity with Add Transaction; multi-period budgets with calendar windows; pip favicon + manifest; portal-rendered notification popover; adaptive `Money` component for billion-scale values; Planner & Chat moved to floating action buttons. |
| 2026-05-10 | [Admin](admin/CHANGELOG.md#v100--production-data-layer--northstar-dashboard-2026-05-10) | **v1.0.0** | First production release. All 5 pages on live Supabase data. Mock layer deleted. Honest `—` placeholders for cohort metrics that need event-tracking. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v631--admin-dashboard-sanitised-every-page-on-live-data-2026-05-10) | v6.3.1 | Cross-ref of the admin v1.0 release. No consumer code changes. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v63--content-module--adminsupabase--global-add-txn-modal-2026-05-10) | v6.3 | Content module (admin authors → consumer reads with search + favorites). Global Add-Transaction modal hoisted to App root. New `/insights` page. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v622--add-transaction-wiring--ga4-2026-05-10) | v6.2.2 | Add-Transaction modal on the Transactions page. GA4 (`G-E3XKWZP850`) added to all 3 entry HTMLs. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v621--households-rls-recursion-hotfix-2026-05-10) | v6.2.1 | Server-side RLS hotfix. The "No API key found" error was actually an HTTP 500 from RLS recursion on `households` ↔ `memberships`. |
| 2026-05-10 | [Consumer](react/CHANGELOG.md#v62--friction-free-signup--module-port-out-2026-05-10) | v6.2 | All 7 v5 stub pages ported to React. Onboarding becomes opt-in. Email verification non-blocking. Trigger fix so new signups always have a household. |
| pre-2026-05 | [Consumer](react/CHANGELOG.md#v75--rules-based-planner--ai-chatbot-pre-2026-05) | v7.5 | Rules-based AI Finance Planner (no LLM). AI Chatbot scaffold with privacy-safe aggregation. |
| pre-2026-05 | [Consumer](react/CHANGELOG.md#v70--onboarding--emi--recurring--notifications-pre-2026-05) | v7.0 | Smart Onboarding (6 templates). EMI re-amortisation engine. Recurring transactions + notifications. |
| pre-2026-05 | [Consumer](react/CHANGELOG.md#v60--react--typescript--recharts-pre-2026-05) | v6.0 | React + TypeScript + Tailwind + Vite + Recharts rebuild. Dashboard / Reports / Transactions ported; 7 other pages stubbed. |
| pre-2026-05 | [Consumer](react/CHANGELOG.md#v41--cloud--auth--multi-household-pre-2026-05) | v4.1 | Cloud / Auth / Multi-Household. Supabase wired behind the `DataAdapter` interface. Roles + invitations + realtime. |
| pre-2026-05 | [Consumer (vanilla)](react/CHANGELOG.md#v41-internal--adapter-refactor-pre-2026-05) | v4.1 (internal) | `DataAdapter` interface introduced on the vanilla shell. Foundation for the cloud release above. |
| pre-2026-05 | [Vanilla shell](#v50--loans-splits-profiles--privacy) | v5.0 | Loans, Splits, Profiles, Privacy. **Final vanilla-shell release** — frozen as of v6.0. |
| pre-2026-05 | [Vanilla shell](#v40--paper-warm-redesign--debt--net-worth--currency--i18n) | v4.0 | Paper Warm redesign. Debt + Net Worth + Multi-currency + i18n. |
| pre-2026-05 | [Vanilla shell](#v30--themes-charts-settings-help-internal--never-shipped) | v3.0 | *Never shipped* — UI was rolled into v4.0. |
| pre-2026-05 | [Vanilla shell](#v20--family-pulse-score-goals-members-insights) | v2.0 | Family Pulse Score, Goals, Members, AI Insights bar. |
| pre-2026-05 | [Vanilla shell](#v10--budgetflow-mvp) | v1.0 | BudgetFlow MVP. The original release. |

> **Why some Consumer versions look out of order:** v7.0 / v7.5 ran as a parallel **major-feature track** alongside the v6.x **integration track**. Going forward (v6.4+) every consumer release is on a single increasing line. See [§ Version provenance](react/CHANGELOG.md#version-provenance--gaps) in the consumer changelog for the full explanation.

---

## Roadmap (cross-app)

Per-app detail in each `CHANGELOG.md` Roadmap section.

| Up next | App | Version | Headline |
|---|---|---|---|
| **next** | [Consumer](react/CHANGELOG.md#v65-planned) | v6.5 | Server-side budget-period storage (DB migration to add `extras jsonb` on `budgets`). GA4 custom events. Transactions pagination. Resend Edge Function for invitation emails. Bundle code-split. |
| **next** | [Admin](admin/CHANGELOG.md#v110--next-planned) | v1.1.0 | Stripe billing wired to `subscriptions`. Event-tracking pipeline unlocks the 8 cohort metrics on the Dashboard. Audit-log JSON-diff viewer. |
| later | [Consumer](react/CHANGELOG.md#v66-planned) | v6.6 | Stripe consumer Settings. Cohort-event tracking client-side hooks (matched to admin v1.1). |
| later | [Admin](admin/CHANGELOG.md#v120-planned) | v1.2.0 | User suspend/reinstate. Invite admin tier from UI. SIEM audit export. |
| later | [Admin](admin/CHANGELOG.md#v130-planned) | v1.3.0 | Google Workspace SSO. VPN / IP allowlist at Vercel edge. |
| **future major** | [Consumer](react/CHANGELOG.md#v70--future-major) | v7.0 | LLM Chat backend (Claude Haiku via Edge Function). LLM-augmented Planner. Multi-device push. |
| **future major** | [Admin](admin/CHANGELOG.md#v200--future-major) | v2.0.0 | Multi-tenant admin (per-org metrics). Embedded Looker Studio. CS conversation linking. |

---

## Vanilla shell history (v1.0 – v5.0)

The vanilla HTML+CSS+JS app that ran from 2025 to 2026. Removed from the working tree in v7.0.1 after all product pages were ported to the React app (`react/`). Full source is preserved in git history. **Final version: v5.0.**

### v5.0 — Loans, Splits, Profiles & Privacy

A substantial feature release that turns FinFlow into a true multi-context financial workspace. Six new capabilities, all designed to stay low-learning-curve.

**New**
- **Private (excluded) transactions** — flag any transaction with `Exclude from reports`. It stays in your history but doesn't appear in expense/income totals, charts, the Pulse Score, or the Net Worth calculation.
- **Payment method library** — 30+ banks, cards, and wallets baked in.
- **Investment & Transfer transaction types** — beyond income/expense.
- **Multi-profile workspace** — Personal · Family · Single Business · Multi-Business types. Profile switcher in the sidebar header. Each profile has its own transactions, budgets, debts, assets, members, and currency.
- **Split payments** — handle group bills without skewing reports. Mark any expense as a split with N participants. Only your share counts as expense.
- **Loan tracker / EMI breakdown** — debts become real loans. New fields: tenure (months), EMI amount, amortization preview. Recording a payment now splits automatically into **interest** (logged as expense) and **principal** (logged as transfer, excluded from expense totals).

**Improved**
- Aggregation engine refactored: `monthlyData` / `totalBalance` / `spendByCategory` / Pulse Score / Net Worth all respect the new types and the excluded flag.
- Help page expanded with new sections: Splits, Investment Tracking, Loan Tracker, Profiles, Privacy.
- Settings page gains profile management.

### v4.0 — Paper Warm Redesign + Debt + Net Worth + Currency + i18n

Major redesign and feature expansion based on the FinFlow Designs wireframes.

**New**
- **Paper Warm** design system (default theme): Coral · Cream · Ink · Sage · Honey · Terracotta.
- Newsreader (italic display) + Inter Tight (UI) + JetBrains Mono (data) typography.
- **Debt management page** (10 debt types, Avalanche/Snowball strategies, payoff calculator with extra-payment cascade, DTI ratio).
- **Net Worth / Balance Sheet page** (10 asset types organized by liquidity, formula display, 4 financial ratios).
- **Multi-currency support** — 12 currencies with locale-aware formatting via `Intl.NumberFormat`, editable USD-base rates table.
- **Localization (i18n)** — 6 languages.
- **Interactive period charts** — Day / Week / Month / Quarter / Year selector with SVG line, bar, and donut charts.
- **Settings page** — profile, theme cards, localization, debt preferences, sync, account stats.
- **Help & Guide page** — 10 collapsible accordion sections.
- **Pulse Score expansion** — now 5 components (Budget 25% · Savings 25% · Goals 15% · Trend 15% · Debt Health 20%).

### v3.0 — Themes, Charts, Settings, Help *(internal — never shipped)*

Light/dark theme switcher, SVG icon set, responsive design, period-based charts, settings and help pages. Shipped HTML and CSS but `app.js` logic was deferred and rolled into v4.0.

### v2.0 — Family Pulse Score, Goals, Members, Insights

Eight increments shipping the foundational MVP defined in the FinFlow architecture spec.

**New**
- **Family Pulse Score™** — composite 0–100 wellness score on the dashboard.
- **Goals page** — 6 goal types, inline progress updates, completion celebration.
- **Family Members** — multi-member household, sidebar avatar strip, member tag on transactions.
- **Recurring transactions** — weekly/monthly/yearly flag with badge.
- **AI Insights bar** — savings rate analysis, budget overage alerts, top-category flag, goal nudges.
- **Smart auto-categorization** — keyword detection on transaction description (`KEYWORD_MAP`).
- **CSV export** — full transaction history download.
- **Enhanced reports** — net worth, monthly trend chart, top categories, savings rate over time.

### v1.0 — BudgetFlow MVP

The original release. Plain HTML + CSS + JS, no build step, no backend, no dependencies.

**Initial features**
- 4 pages: Dashboard, Transactions, Budgets, Reports
- Single-user, localStorage-only
- Income / expense transaction tracking with 16 categories
- Monthly budget limits per category with color-coded progress bars
- Reports: all-time totals, monthly summary table, top expense categories
- Dark theme with indigo accent
- Modal-based add/edit for transactions and budgets
- Auto-seed demo data on first run

---

## Versioning conventions

Each app uses standard SemVer (`MAJOR.MINOR.PATCH`):

- **Major** (`x.0.0`): breaking changes to data shape, design language, or core architecture.
- **Minor** (`x.y.0`): additive features, no breaking changes, backward-compatible.
- **Patch** (`x.y.z`): bug fixes, polish, copy edits, server-only hotfixes.

Cross-app changes are recorded in **both** apps' changelogs with explicit cross-references. The version number doesn't have to bump in both apps for a change that touches one and is referenced from the other.
